# secrets_class.yaml — Approved secrets source paths for complete server restoration
# Each path here is backed up to the SECRETS crypt remote, encrypted
# with a SEPARATE passphrase stored only in the password manager.

paths:
  - /etc/ssl
  - /etc/apache2/ssl
  - /root/.acme.sh
  - /root/.ssh
  - /home/adc/.ssh
  - /home/solace/.ssh
  - /etc/wireguard
  - /etc/letsencrypt
  - /opt/adc/secrets
  - /opt/adc-backup/rclone.conf
